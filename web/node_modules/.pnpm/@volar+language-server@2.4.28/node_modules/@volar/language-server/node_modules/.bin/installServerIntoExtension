#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../vscode-languageserver/bin/installServerIntoExtension" "$@"
else
  exec node  "$basedir/../../../../vscode-languageserver/bin/installServerIntoExtension" "$@"
fi
# cmd-shim-target=/Users/ezequielmenor/Proyectos/Stackd/stackd/web/node_modules/.pnpm/@volar+language-server@2.4.28/node_modules/vscode-languageserver/bin/installServerIntoExtension
